var материалы = null;

function создатьМатериалыЦветов()
{
    материалы = {};
    ??? (var имя in цвета)
    {
        var м = ???.материалы.создать???(имя);
        м.задатьШейдеры(???Вершинный, ???Фрагментный);
        var цвет = цвета[имя];
        м.задатьЮниформ("color", цвет);
        материалы[имя] = м;
    }
}
